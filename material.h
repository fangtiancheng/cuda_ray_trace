#ifndef _MATERIAL_H_
#define _MATERIAL_H_
#include "ray.h"
// #include "hittable.h"
/*
class material {
public:
    __device__
    virtual bool scatter(
        const ray& r_in, const hit_record& rec, color& attenuation, ray& scattered
    ) const = 0;
};
*/
#endif